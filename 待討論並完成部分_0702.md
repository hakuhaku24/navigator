# 待討論並完成部分 — 2026-07-02

> ⚠️ **注意**：部分引用的 `.md` 文件可能已過時。`DEMO_PATH.md` 最後更新於 2026-05-26，`CLAUDE.md` 的「當前進度」章節停在 Phase 2 描述，兩者均早於 2026-06-26 的 TDX / RAG 大幅進展，內容未必反映目前實際狀態。以 `DEVLOG.md` 為最可信的進度來源。

---

## 一、需要大家配合的事（來源：2026-04-19 ～ 2026-06-30 白話說明）

1. **⚠️ Supabase 連線異常，需要盡快確認** — `後端`
   不是單純休眠，是資料庫網址連 DNS 都解析不到，可能是專案被刪除或網址換掉了。請有後台管理權限的組員盡快確認狀況，這會卡住所有需要寫入／讀取資料庫的工作（PTT／官網／YouTube 查證資料寫入、TDX 資料正式匯入、新欄位套用）。

2. **套用 TDX 新欄位** — `後端`
   今天寫好的資料庫設定檔還沒套用，需要先跟大家說一聲、確認沒人正在同時跑舊版匯入程式，避免互相打架弄亂資料。

3. **預訂資訊（booking enrichment）** — `後端`
   規劃文件已寫好，但實作還沒開始，等排進後續排程。

---

## 二、接下來需要大家配合的事（來源：2026-06-04 ～ 2026-06-30 白話說明）

1. **⚠️ Supabase 連線異常，需要盡快確認**（同上，Supabase 優先）— `後端`

2. **套用新的資料庫欄位**（同上，確認沒人跑舊版匯入後套用）— `後端`

3. **正式拍板「我們的施力點是什麼」**
   建議跟教授明確說「我們選的施力點就是可信景點資料庫 ＋ 應變系統」，並說明其他功能是刻意暫停、不是沒做完，回應教授「分工太發散」的印象。

4. **跑一次真正大量的 TDX 匯入** — `後端`
   不要只測幾筆，拿實際數字（成功匯入幾筆、成功率多少）回答教授對規模化的疑慮，這是目前最容易拿出來的具體成果。

5. **畫一張整合流程圖**
   景點驗證 → 資料庫 → 應變系統 → 使用者畫面，讓大家（包含教授）一眼就懂各模組是怎麼串起來的。不需要大改程式碼，先把故事講清楚。

6. **對 LLaMA 本地部署、KKday／Klook 抽成這兩件事給個明確答案** — `後端`
   要嘛排進時程，要嘛跟教授說明已改變方向、原因是什麼，別讓這兩個 open question 一直沒人處理。

---

## 三、教授回饋建議（2026-07-01 當次課）

### 建議一：多來源資料衝突時，允許並存、不要強制解決

> **✅ 後端＋前端已完成（後端 2026-07-02／前端 2026-07-05）**

教授指出爬到的不同部落格或網頁資訊必然有衝突（如開放時間不同、景點存在性），系統必須有機制處理。

- 若能澄清（有更新時間戳、官方來源優先），自動選擇可信度最高的版本
- 若無法澄清，**讓多版本並存**，不要強制輸出單一答案
- 目標不是「解決衝突」，而是「讓演算法知道衝突存在」，並透明呈現給使用者

已實作：`agents/poi-verifier/src/conflict-resolver.ts`
- 兩步驟解法（Step 1 澄清 / Step 2 並存）完整落地
- 支援 TDX 政府資料作為 official 層，可解決 OSM 誤比對與台灣地名異體字問題
- 通過全部 45 筆 POI 測試（32 筆有衝突、13 筆乾淨），詳見 `tests/conflict-resolver-report.md`
- `ConflictRecord<T>` 的 `variants` 陣列保留所有版本溯源，`is_conflicted: true` 供前端顯示警告

**前端 UI 已完成（2026-07-05）**：`src/app/(app)/explore/page.tsx`
- `agents/poi-verifier/generate-frontend-conflicts.ts` 離線重算全部 45 筆的 `ConflictAnalysis`（零額外 API 呼叫，純用既有 `raw_sources` 資料），輸出 `results/poi_conflicts.json`
- `src/data/gen-poi-kb.js` 合併進 `poi-kb.ts`，新增 `conflicts` 欄位（含 `ConflictRecord<T>` / `SourceVariant<T>` 型別）
- 景點卡片：有衝突欄位就顯示「⚠ 資料分歧」amber 徽章（實測 32 張卡片，與後端測試報告的 32 筆衝突數一致）
- 詳情面板：新增「來源資料不一致」區塊，列出每個衝突欄位採用的最終值、判定方式（單一來源／來源一致／依層級澄清／依時效性澄清／並存）、以及各來源的原始值＋來源層級（官方／半官方／部落格／使用者回報）
- 型別檢查（`tsc --noEmit`）與 `next build` 皆通過，dev server 實測 server-rendered HTML 確認徽章數量正確

`後端` ✅ — `前端UI` ✅ 全部完成，本項不再需要討論

---

### 建議二：LLM 要真正「用到」我們驗證好的 POI 資料

> **🔧 後端部分完成 — 前端整合需討論後再動工**

教授追問：Demo 場景雖然運作，但**使用者端的 LLM 呼叫是否真的讀得到我們驗證後的 POI？**

- 若用一般 LLM API（直接呼叫 Gemini），不會自動讀到我們的資料庫
- 必須透過 **RAG（向量檢索 ＋ prompt 注入）** 或地端部署才能讓 LLM 看到這些資料
- 目前 RAG Reranker ＋ Hybrid Search 已完成（agent 內部使用），但**尚未整合進前端 `/api/` Route Handler**

⚠️ **需討論後才能動工**：Route Handler 的整合方式（串接哪個 agent endpoint、前端 UI 如何呈現 LLM 回應）需要先跟組員對齊，否則前後端會做出不相容的接口。

`後端` 🔧 部分完成 — Route Handler 端到端整合 ⏳ 待討論 — `前端UI，可能會和計畫的不同` ⏳ 待討論

---

### 建議三：跟既有相似系統比較，找出我們的差異化

> **⏳ 待討論 — 實作前需要先做市場調查**

旅行資料庫的收集與驗證應先調查是否已有人做過類似的系統。

- 若別人做過，實作時不要把重點放在資料收集，除非這是不可或缺的基礎建設
- 調查方向：TripAdvisor、Google Places、MindTrip、WanderNest 等的 POI 驗證機制
- 重點確認：我們的「L0–L3 分級 ＋ 應變邏輯」是否是市場上沒有的

⚠️ **需討論後才能動工**：這件事本身沒有程式碼要寫，但結論會直接影響 Demo 的說法與投影片措辭。建議全組開一次短會（30 分鐘），分配 2-3 個系統各自調查，匯整後拍板「我們的獨特性說法」。沒有共識前不要各自在 README / 報告書裡寫不一致的比較。

---

### 建議四：主打 Alternative Route / Schedule 作為核心差異化

> **⏳ 待討論 — 主軸未定前前後端都不要動**

教授肯定 Alternative Route（應變行程）的方向，在 POI 資料可靠的前提下，**應變排程**是具說服力的差異化功能。

- 期末展示或比賽**主打哪個功能**需要決定：POI 驗證可靠性 vs. 應變排程能力
- 建議二擇一作為主軸，另一個作為支撐
- 8 步 Demo 流程可定位為「情境故事背景」，選定**天氣應變 Swap（Step 7）**作為深度展示主軸

⚠️ **需討論後才能動工**：Strategy Agent 的 Swap UI mock 以及前端天氣觸發場景，設計方向強烈依賴「我們主打什麼」的結論。若尚未決定主軸就開始做前端，極可能做完又要改。建議與建議三的討論合併進行，同一次會議拍板方向後，前後端才一起動。

`前端UI，可能會和計畫的不同` ⏳ 待討論 — Strategy Agent Swap UI mock（固定觸發天氣場景）— `後端` ⏳ 待討論
