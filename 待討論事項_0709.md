# 待討論事項與優先順序 — 2026-07-09

> ⚠️ 會議記錄，早於 2026-07-16 減法拍板，部分項目（如 #4 提到的 Architect Agent）已被後續決議取代或改稱——見 `0716_減法決策與不做清單.md`、`系統架構圖_競賽版.md`。「Architect Agent」不是 LLM agent，是純規則的行程排序邏輯，不要依此文件的舊敘述動工。

> 前身為 `待討論事項_0705.md`。本次更新反映兩件事：(1) Supabase 連線異常已排除，原 P0 阻塞項移除；(2) `系統架構圖_0709.md` 已產出並經教授核可，原本待畫的整合流程圖項目視為完成；(3) 7/8 教授講評帶來新的優先順序調整，詳見下方各項與文末「教授提醒」。
> 編號 = 優先順序，**數字越小越優先**、越可能是專案卡關的原因。同一個優先級內用小數區分（1.1、1.2…）。
> 來源對照：延續自 0705 文件編號會標註「原#x」；教授 7/8 講評新增的項目標註「新增（教授0708）」。

---

## 🔄 2026-07-23 更新總覽（讀這節就能知道下面哪些還沒過期）

> 依 `docs/decisions-0707.md`、`0716_減法決策與不做清單.md`、`DEVLOG.md`（2026-07-15～21 三筆）、`NIICC_初審文件_企劃書.md`、`CLAUDE.md` §9 進度矩陣核對後更新。教授 07/15、07/22 講評的新重點已併入對應章節，並在文末新增「教授 0715／0722 新增討論項」。

**已拍板／已完成，本文件下方對應項目已標註 ✅**：
- P1 #3 Demo 主軸敘事 → 已定案主打天氣應變 Swap（`docs/decisions-0707.md` 決策1），並在 `系統架構圖_競賽版.md`、`NIICC_初審文件_企劃書.md` 中落實。
- P1 #4 LLM×RAG 前端串接 → 後端完成且**已接前端**：`explore` 頁接 `/api/poi/search`（list 模式，真資料）、`/api/contingency` 預設走 Supabase RPC 真檢索（`fallback_pool` 只作安全網）。**但語意搜尋框仍未接**——`query` 參數目前沒有任何前端入口會送出，這部分還沒做完，見下方 #4 更新。
- P1 #5 商業模式 + 找比賽簡章 → 已定案報名「2026 全國大專校院智慧創新暨跨域整合創作競賽（NIICC）」，`NIICC_初審文件_企劃書.md`／系統需求書已依教授 07/15「捨棄獨立 App、改走 Plug-in 外掛服務」意見改寫定稿（授權條款、送件日期等收尾細節待補）。
- P2 #6 施力點對外說法同步 → 已同步進 README／NIICC 文件（可信景點資料庫 ＋ 即時韌性應變，Plug-in 定位）。
- P2 #7 LLaMA 地端部署 → 已拍板放棄（`docs/decisions-0707.md` 決策3），本文件當時列為 open question 屬資訊落後，現已補正為已解決。

**仍未解決、持續是全隊最大瓶頸**：
- 🔴 P0 #1 資料涵蓋範圍拍板（擴大 vs 集中）——**兩週過去仍未拍板**。`NIICC_初審文件_企劃書.md`「四、計劃管理」把它列為階段 1 待辦，`poi_catalog` 目前仍卡在 45 筆（見 `DEVLOG.md` 2026-07-21）。這是目前唯一還在阻塞後續所有資料規模決策的項目，本次更新維持 P0。
- 🔴 #11 ingestion signals 缺欄 bug **仍未修**——直接複查 `agents/poi-verifier/batch-verify.ts:135`、`ingest-from-results.ts:58`，`ingestToDB()` 呼叫仍然沒帶 `signals` 參數，`category`／`image_url`／`website_url` 依然會在回填時被丟棄。`ingest-with-p012.ts` 那條路徑本來就有正確傳遞，不受影響。因為 #1／#2／#10（TDX 匯入與既有 45 筆回填）都要靠 `ingestToDB()`，這個 bug 不修，範圍一拍板就會立刻卡下一步，**建議提升為 P0 附屬阻塞項**，理由與詳情見下方 #11。
  - ⟶ **2026-07-26 修正（範圍已縮小，嚴重度下修）**：呼叫端確實仍只傳兩參數（這點不變），但「三個欄位都被丟棄」已不正確——`src/ingestion.ts:302-313` 早已有 fallback：無 signals 時 `category` 退回 `osm.category`、`website_url` 退回 `official_website.url`，兩者仍會正確寫入。**實際仍被丟棄的只剩 `image_url`／`images`**（只有 signals 會填）。故 #11 已縮小為單一 `images` 欄位缺口，不再是「P0 附屬阻塞項」等級；詳見下方 #11 的 2026-07-26 修正註記。
- 🔴 **新發現（2026-07-24 pipeline 實查）：現行 `poi_verified.json` 45 筆有 30 筆是靜默降級的 fallback，不是真實驗證**——批次跑到一半 Gemini Free Tier 配額耗盡，後 30 筆走「LLM 不可用 → 預設 L2」分支（`facts=null`、L2 是預設非判斷），而輸出沒標記降級狀態、下游分不出真假。這牽動 #1（範圍拍板時能不能信任既有 45 筆）與 #10（重新匯入前必須先重跑那 30 筆，否則把降級資料一起 embed）。詳情見 `agents/poi-verifier/KNOWN_ISSUES.md`（三筆 2026-07-24 條目）與下方 #14（2026-07-24 pipeline 實查）。**修法是非 skill 的前置改動：把 `llm_source` 傳進輸出 + Tier 1 綁卡重跑那 30 筆（< NT$1），應早於任何一次重新匯入。**
  - ⟶ **2026-07-26 更新**：其中「把 `llm_source` 傳進輸出」的程式改動**已完成**（`src/agent.ts`／`src/types.ts` 已持久化 `llm_source`，keyless 整合測試驗證輸出含 `"llm_source": "fallback"`；同批一併修掉 L0 backup_logic 掛回、移除 enrich prompt 會被丟棄的 `backup_logic`）。**仍待辦**：Tier 1 綁卡重跑那 30 筆——牽涉花費與 #1 範圍決議，需團隊確認後執行。既有 `poi_verified.json` 45 筆尚未重跑，仍不含 `llm_source`。

**新增（教授 07/15、07/22 講評帶出的新項目）**：見文末新章節「🆕 教授 0715／0722 新增討論項」，重點是「Benchmark／A-B 對照測試設計」與「Harness Engineering（Skill 路由）要做到什麼程度」兩項，建議下次會議一併排入。

**新增（本文件先前遺漏，補列）**：
- P1 #4.5「天氣應變接上使用者實際建立的行程」——`Navigator_週報_0715-0721.pdf`「下週規劃」與 `CLAUDE.md` §9「下一步如果要選一個做」都把這件事列為性價比最高的缺口，但先前版本的本文件沒有收錄，這次一併補上。
- #16「具體商業獲利模式與規模化故事」——先前把商業模式整項標記已完成（#5），但複查發現送件文件目前只有 Plug-in **概念定位**，沒有具體獲利數字，這塊教授 07/15 明確要求的「千萬產值」論證其實還是空白，優先度不低，建議跟 #5 一起排。
- #17「RAG Chunking 是否適用」、#18「Context Window／Hand-off 機制是否要做」、#19「開發成本管控」、#20「解耦設計優勢敘事補寫」——教授 07/22 提過但先前版本完全沒收錄的四個小點，詳見對應章節，多數只需要一句話防守或 10 分鐘內補寫即可結案，不是大工程。

---

## 🔴 P0 — 阻塞全隊，今天/本次會議必須有結論

### 1. 資料涵蓋範圍拍板：擴大 vs 集中驗證 `全隊` 新增（教授0708） ⚠️ 仍未拍板（2026-07-23 複查）

- **現況**：目前 demo 資料僅 45 筆。教授 7/8 直接點出本專題的核心賣點是「用堪用的技術，涵蓋全台灣的景點」——廣度與資料量是差異化優勢，而非技術本身有多先進。教授要求針對資料量不足要有明確 Plan，並給了兩個方向：擴大景點涵蓋範圍，或縮小並集中（例如只鎖定中壢／央大附近）在小範圍內做到完整驗證。
- **為什麼最優先，而且拖了兩週還在拖**：`poi_catalog` 截至 2026-07-21（`DEVLOG.md` 最新確認）仍然只有 45 筆，009 新增的事實欄位全是 NULL。`NIICC_初審文件_企劃書.md` 已經把「資料涵蓋範圍拍板與 TDX 正式匯入」寫成計畫階段 1，代表送件文件都已經在倒數計時等這個決議，但決議本身還沒有發生。這個決議直接決定 TDX 匯入、POI 驗證 pipeline 要往哪個地區／規模擴充，也牽動送件文件「計劃管理」章節的真實時程（目前是佔位日期）。
- **今天要決議**：擴大還是集中？若集中，鎖定哪個地理範圍（中壢／央大周邊？）；若擴大，用什麼節奏與自動化程度去擴充。**建議搭配 #11（signals bug）與 #2（TDX 排程）一起拍板，因為拍板當天如果想立刻動手匯入，#11 沒修會直接前功盡棄。**

---

## 🟠 P1 — 阻塞前端/後端下一步方向，不拍板會做出不相容或用不到的東西

### 2. TDX 正式匯入排程 `後端` （原#2，因 Supabase 已恢復、migration 008/009 已套用而改變性質） ⚠️ 仍待執行

- **現況**（2026-07-23 複查）：Supabase 連線異常已排除，且比 0709 當時更進一步——**migration 009（`hybrid_search_return_facts`）也已套用並驗證**（`DEVLOG.md` 2026-07-21：線上 RPC 已回傳 `source_id/description/address/lat/lng/category/city/hours/website_url/tags/images` 等完整事實欄位）。`poi_catalog` 資料量本身完全沒變，仍是 45 筆，新欄位仍為 NULL——schema 與 RPC 全部就緒，純粹是還沒有人真的執行大規模匯入。TDX OAuth 憑證已驗證可用，`ingest-from-tdx.ts` dry-run 正常，隨時可以真的執行匯入。
- **為什麼優先**：純粹卡在 #1（範圍拍板）與 #11（signals bug 修復）兩件事，工具鏈本身沒有阻塞。範圍沒拍板就先跑，很可能匯入到不需要的地區或筆數不夠，要重做；signals bug 沒修就跑，回填的資料一樣是壞的。
- **今天要決議**：#1 拍板後，由誰執行 `npx ts-node ingest-from-tdx.ts`、跑多大規模（`--top`／`--city` 參數）、跑完後誰驗證資料正確；並確認 #11 已先修完。

### 3. ~~Demo 主軸與敘事更新：可信資料庫廣度 vs 應變排程能力~~ `前端` `後端` （原#3，依教授意見調整框架）✅ 已拍板（2026-07-07）

- ~~教授 7/8 明確表態本專題最大價值在於「用堪用技術涵蓋全台灣景點」……今天要決議：主敘事怎麼講；8 步 Demo 流程是否仍鎖定 Step 7。~~
- 已在 `docs/decisions-0707.md`「決策1」定案：**主打天氣應變 Swap（Step 7），POI 可信度作為支撐論述**，理由是應變路徑會走完整條「驗證 → poi_catalog → 應變 → UI」骨幹，一個 demo 同時回應「各模組各做各的」疑慮。此定位已延續進 `系統架構圖_競賽版.md` 與 `NIICC_初審文件_企劃書.md`（差異化比較表明確寫出「沒有一家做行程進行中、以 L0–L3 錨點分級為約束的即時備案替換」）。此項移出討論清單，維持已完成狀態。

### 4. LLM × RAG 前端串接與呈現方式 `前端` `後端` （原#4，架構已定案，落實待決） ⚠️ 部分完成——list 模式已接，語意搜尋框仍未接

- **現況（2026-07-23 複查）**：
  - ✅ **已完成**：`explore` 頁已串接 `/api/poi/search`（list 模式，真實資料，含信度分數／來源徽章／衝突呈現）。`/api/contingency` 應變候選池預設走 Supabase RPC 真語意檢索（`pool_source: 'supabase_rpc'`），靜態 45 筆只作為 RPC 失敗時的 fallback，不是主要來源。這部分已經回答了教授「使用者端 LLM 到底有沒有讀到我們驗證的資料」的質疑。
  - ❌ **仍未完成**：`searchPois()`（`agents/poi-verifier/src/poi-search.ts` 對應的 `src/lib/poi-search.ts`）支援的**語意搜尋**（自然語言 query，如「下雨天想找室內景點」）後端已經寫好，但前端目前沒有任何搜尋框會把 `query` 參數送出去——`explore` 頁只送 list 模式（見 `CLAUDE.md` §9 進度矩陣）。
- **為什麼還要繼續追**：語意搜尋是 RAG 混合檢索（漏斗式檢索、Token 成本控管）唯一使用者直接感知得到的入口，目前完全沒有 UI 觸發，等於教授看不到這條技術路徑的實際效果。
- **今天要決議**：`explore` 頁要不要加一個搜尋框把 `query` 傳給 `/api/poi/search`，呈現方式（含引用來源／信度標示）怎麼做；如果評估後決定不做這個 UI，也要明確記錄理由（例如時間不夠、demo 不需要）。

### 4.5. 天氣應變接上使用者實際建立的行程 `前端` `後端` 新增（2026-07-23 補列，源自週報＋CLAUDE.md 進度矩陣）

- **現況**：`weather` 頁（`trip/[id]/weather`）目前不讀 `loadDraft(tripId)`，網址上的 `[id]` 沒有作用——不管使用者用 FFR13 選點組出什麼行程，天氣應變頁永遠評估同一組寫死的 demo 景點（北海岸放空團），跟使用者真正建立的行程無關。2026-07-21 已讓「接受替換」正確持久化，但只存在 demo 專用的 localStorage slot，不會寫回真實行程草稿（`CLAUDE.md` §9 進度矩陣「天氣應變 ×『你自己建的行程』」列為 ⚠️ 兩者沒接在一起）。
- **為什麼是目前性價比最高的缺口**：後端管線（CWA 真偵測 → 期望值 → RAG 候選 → 反思審查 → LLM 敘述）已經是全真實資料，`draft-itinerary.ts` 選點組行程（FFR13）也已經是真實資料，**唯獨這兩條已完成的真實鏈路彼此沒有接上**。只要把輸入換成 `loadDraft(tripId)` 而非固定 demo 陣列，就直接解決「demo 出來的行程沒辦法展示天氣應變」這個明顯落差——`Navigator_週報_0715-0721.pdf`「下週規劃」與 `CLAUDE.md` §9 都獨立把這件事列為第一優先建議。
- **今天要決議**：這次要不要排進 sprint、由誰做；如果要做，順帶確認「接受替換」的持久化要不要一併從 demo-only slot 改為寫回真實行程草稿（否則會出現「讀到真行程、但存不回真行程」的半套狀態）。

### 5. ~~商業模式重新檢視 ＋ 尋找比賽簡章~~ `全隊` （原P2 #6 KKday 部分，因教授7/8意見與明確期限提升）✅ 已完成

- ~~教授質疑原本設想的 KKday／Klook 抽佣模式……並要求下週開會前找到合適的比賽簡章。~~
- 已定案報名「2026 全國大專校院智慧創新暨跨域整合創作競賽」（NIICC），並依教授 07/15 意見「捨棄獨立 App、改走 Plug-in 外掛服務」全面改寫商業敘事——`NIICC_初審文件_企劃書.md` 明確定位系統的價值出口是 `/api/poi/search`、`/api/contingency` 兩個可被旅遊平台串接的 Plug-in 端點，而非獨立下載 App。企劃書與系統需求書已完成初稿並在 2026-07-16 對齊減法決策後定稿（commit `2c450fc`）。
- **收尾細節（不影響拍板結論，送件前需人工過一遍）**：授權條款待團隊決定、「四、計劃管理」章節的起訖日期仍是佔位（待填入真實截止日／demo 日）、全程匿名檢查需人工複查簡報 PPT／demo 影片是否帶到系所或姓名。
- **和泰 AI 黑客松主題比對（教授0715 提醒）不適用**：教授 07/15 提醒過如果參加和泰的比賽要確認主題（行銷）是否緊扣，但團隊實際選擇的是 NIICC，不是和泰，這條提醒本身沒有變成待辦，記錄於此純粹是把 07/15 逐字稿裡這條意見明確結案，避免之後有人誤以為還有一個和泰的申請在跑。若之後真的要投和泰或其他企業命題賽，屆時要重新檢查主題對齊。

---

## 🟡 P2 — 需要方向共識，但不會立刻卡住寫 code

### 6. ~~跟教授同步「施力點」對外說法~~ `全隊` （原#5，教授已給方向，改為同步事項）✅ 已完成

- ~~教授 7/8 已直接肯定「可信景點資料庫 ＋ 堪用技術涵蓋全台灣」是核心賣點……~~
- 已同步進 `NIICC_初審文件_企劃書.md`（開頭即以此定位破題）與 README。`docs/decisions-0707.md` 也留有一段可直接對教授複誦的施力點說法。此項無剩餘工作。

### 7. ~~LLaMA 地端部署 open question~~ `後端` （原#6 剩餘部分）✅ 已拍板（2026-07-07，早於本文件成文，屬資訊落後已補正）

- ~~LLaMA 地端部署是否排入時程，一直沒人給明確答案。~~
- 已在 `docs/decisions-0707.md`「決策3」拍板**放棄地端部署**：原始動機「讓 LLM 讀到我們的資料」已被 RAG 注入（#4）解決，且地端部署需要 GPU 資源與維運，對畢業專題邊際效益為負。若期末後要研究，方向是「離線 demo 備援」而非主流程。本文件當初列為 open question 是資訊沒同步，非真的未拍板，現已補正。

---

## 🟢 P3 — 已有共識或屬於執行細節，可並行處理、不需要專門開會

### 8. ~~多來源衝突的前端 UI 呈現~~ `前端` （原#7）✅ 已完成（2026-07-05）

- ~~後端衝突解析已完成（`conflict-resolver.ts`，45 筆全測過），只差前端怎麼標示「這個景點資料有分歧版本」。~~
- 已在 `src/app/(app)/explore/page.tsx` 實作：景點卡片顯示「資料分歧」徽章，詳情面板列出每個衝突欄位的採用值、判定方式與各來源原始值。資料由 `agents/poi-verifier/generate-frontend-conflicts.ts` 離線算出。教授 7/8 也認可這類「有判定規則即可」的做法，不需要再花時間追求絕對精準，此項維持已完成狀態即可。

### 9. ~~整合流程圖（景點驗證 → 資料庫 → 應變系統 → 使用者畫面）~~ `全隊` （原#9）✅ 已完成（2026-07-09，並於 2026-07-20 依 0716 減法決策重出競賽版）

- ~~不需要改 code，找個人畫一版初稿，其他人 review 即可。~~
- 已產出 `系統架構圖_0709.md`，涵蓋外部資料來源 → POI Verifier Agent → 知點資料庫（含 pgvector Hybrid Search）→ Architect / Contingency Handler Agent → 使用者介面的完整流程與回饋迴路，並經教授核可。**後續更新**：因 0716 減法決策移除多人規劃、「Architect Agent」一詞停用，`系統架構圖_競賽版.md` 已重畫無投票版架構圖，並於 2026-07-20 補上反思迴路升級路徑（Loop Engineering 落點）與規則覆蓋權敘述，另出 PDF（`Navigator_競賽版架構圖_0720.pdf`）。此項移出討論清單。

### 10. 跑一次大量 TDX 正式匯入 `後端` （原#8）⚠️ 仍待執行，卡在 #1 / #11

- 純執行任務，不是決策。Supabase 與 migration 009 都已就緒，等 #1（範圍拍板）與 #11（signals bug 修復）確定後直接跑，拿實際成功率數字回報教授即可。與 #2 是同一件事的不同視角，一併排程。

### 11. 既有 45 筆回填 signals 缺口（Bug）`後端` 新增（2026-07-09 複查發現） ⚠️ **2026-07-23 複查：仍未修復，建議提升優先度**

> **2026-07-26 複查修正（範圍已縮小為單一欄位）**：呼叫端 `batch-verify.ts:135`／`ingest-from-results.ts:58` 確實仍只傳兩參數、未帶 `signals`（不變）；但下方「`category` 與 `images` 會寫入空值」的結論已過時——`src/ingestion.ts:302-313` 早已加了 fallback：無 signals 時 `category` 退回 `osm.category`、`website_url` 退回 `official_website.url`，兩者都會正確寫入。**真正仍被丟棄的只剩 `image_url`／`images`**（只有 signals 會填）。因此若只是要回填既有 45 筆的 `category`／`website_url`，現況已可正確寫入，不必先改呼叫端；剩下的 `images` 若要補，才需要讓非 TDX 呼叫端傳 `signals.image_url`（惟現行 45 筆手寫資料的 `raw_sources` 未必帶圖，補了可能仍為空）。下方原始 2026-07-23 內容保留供對照。

- **現況**：確認 migration 008/009 已套用後，原本想直接把既有 45 筆重新跑一次入庫來回填新欄位，但檢查 `ingestToDB()`（`agents/poi-verifier/src/ingestion.ts`）與呼叫端後發現：**現在直接重跑既有 45 筆，`category` 與 `images` 這兩個新欄位還是會寫入空值**——不是資料不存在，是呼叫端沒把資料傳進去。
  - `batch-verify.ts:135`、`ingest-from-results.ts:58` 呼叫 `ingestToDB()` 時**至今仍然**完全沒帶 `signals` 參數（2026-07-23 直接讀原始碼複查確認，兩處呼叫式維持 `ingestToDB(result, { sourceId, region })` 兩參數，沒有第三個 `signals` 引數），即使原始資料每筆都有 `category` 與 `image_url` 也會被丟棄。
  - ✅ **例外／已正確的路徑**：`ingest-with-p012.ts:156` 有正確組出 `IngestSignals` 並帶入 `ingestToDB(poi, { sourceId, region }, signals)`，這條路徑（P0/P1/P2 訊號整合）不受影響。
  - 不受影響（原本就沒問題）：`city`／`curated_zone` 是 `ingestToDB()` 內部從 `region`／`address` 自動推導，不靠 signals，回填時會正確。`hours`／`phone`／`zip_code` 本來就不在 45 筆手寫資料裡，維持 null 合理，不是 bug。
- **為什麼要修，而且比 0709 當時更急**：`NIICC_初審文件_企劃書.md`「計劃管理」已經把「先修 ingestion signals 缺欄」明文寫進階段 1 的必要工作，代表這不再只是內部技術債，是送件文件已經承諾要做的事。如果 #1 一拍板就直接用 `batch-verify.ts`／`ingest-from-results.ts` 這兩條路徑重跑既有 45 筆或新匯入資料，會誤以為「已經回填過」但 `category`/`images`/`website_url` 其實還是空的，等於再浪費一輪 embedding API 成本才發現。
- **今天不用決議**：純程式修正，比照 `ingest-with-p012.ts` 的做法，在 `batch-verify.ts`／`ingest-from-results.ts` 補上 `signals` 裡 `category`/`image_url`/`website_url` 的傳遞即可，不需要開會；但要在真正執行既有 45 筆回填或大規模 TDX 匯入（#2／#10）之前完成，否則回填等於白做。

### 12. Booking enrichment 實作排程 `後端` （原#10）🧊 已改為不排入（2026-07-16 減法決策 + CLAUDE.md §7 明列 out of scope）

- 原本規劃是「排進哪個 sprint」的問題。0716 減法決策後，`CLAUDE.md` §7 已把「商家串接、付款」明列為期末不做項目，`collection`／`settings`／`dashboard` 也在 §7.5 凍結清單中不投工時。此項不再需要排程討論，除非之後範圍決議（#1）明確擴大到需要 booking 資訊才重新評估。

---

## 🆕 教授 0715／0722 新增討論項（本次新增，供下次會議排入）

> 07/15、07/22 兩次講評帶出不少新的技術與定位要求，多數已經在 `NIICC_初審文件_企劃書.md` 裡有文字對應（三層架構、反思迴路、Plug-in 定位、核心功能聚焦都已落實），下面只列**還沒有明確結論或還沒動工**的部分，避免整份文件重複。
>
> **2026-07-23 二次複查追加 #16–19**：第一輪更新時漏掉幾個教授提過但本文件從未收錄的點，複查後補上，來源逐項標註。

### 13. Benchmark／A-B 對照組測試設計 `全隊` 新增（教授0722）

- **現況**：教授 07/22 具體要求兩件事——① 用真實世界的極端情境（教授舉例：韓文地鐵站名轉換）當壓力測試題；② 設計 A/B 對照組（一組只給 AI 三句模糊指令，另一組給系統整理好的完整 context），用產出結果的落差證明「資料層有沒有減少 AI 幻覺與無用回答」。目前 repo 內只有工程測試（`tests/narrative-checker.test.ts`、`tests/reflection-loop.test.ts` 等 19 assertions、`tests/tdx-pipeline.test.ts` 88 assertions），**沒有針對「有資料層 vs 沒資料層」設計過的效果對照測試**。
- **為什麼值得做**：教授同時點出「目前業界/學界未必有一套針對旅遊 AI 應用的標準檢測機制」，暗示這件事本身可以是一項有公信力的產出，不只是內部品保，也是可以寫進企劃書／簡報的加分項。
- **建議討論**：要不要做、做到什麼規模（例如挑 5–10 題涵蓋室內外/天氣/地址模糊等情境，跑一輪「裸提示 vs 有 RAG context」對照，記錄幻覺率或正確率），由誰負責、排在哪個 sprint。

### 14. Harness Engineering／Skill 路由要做到什麼程度 `後端` 新增（教授0722）

- **現況**：教授 07/22 舉例 Google 的 Skill 框架，希望系統有一個「主檔（Master Routing）」＋數十種預先定義的 Skill，依問題判斷呼叫哪個 Skill。目前系統的 Harness 化程度是**單一 LLM 呼叫層級**的——每個 LLM 呼叫都有 schema 驗證、重試、防截斷（`NIICC_初審文件_企劃書.md` 已寫明這點），但沒有教授描述的那種「多技能路由」架構；現況是固定的兩個 Agent（POI Verifier、Contingency Handler）各自跑一條線性流程，不是動態路由到 N 個 skill。
- **為什麼要討論而不是直接動工**：07/08 教授才說過「技術到堪用水準就該往下一階段推進、不要在架構複雜度上鑽牛角尖」，07/22 又提出更精細的框架要求，兩次意見方向不完全一致。**建議先跟教授確認**：這是「加分項、時間夠再做」還是「現有兩 Agent 線性流程在他眼中就不算數」，避免在 P0 #1／#11 都還沒解決時，把工時錯配到架構複雜化上。
- **如果決定要做的最小版本**：不用真的拆 22 個 skill，比較務實的落地方式可能是把 POI Verifier 內的驗證器（`validators/`）、Contingency Handler 內的偵測器（`detectors/`）包裝成明確命名的 skill 清單，並在 README／架構圖標注「呼叫哪個 skill 對應哪段程式碼」，用現有模組化程度包裝敘事，而不是重寫架構。
- **2026-07-24 試做後發現的落地誤區（供下次確認範圍時參考，已還原、未進 repo）**：曾先跳過「建議先跟教授確認」直接動工，理由是「07/22 比 07/08 新，應該以較新意見為準」——但這個推論混淆了兩件事：日期較新不代表意見取代舊意見，07/08「別在架構複雜度上鑽牛角尖」與 07/22「要有 Master Routing＋數十種 Skill」的落差，本質上是**範圍認知落差**，不是「舊版意見被新版蓋過」，不能用日期推翻，本項上一行「建議先跟教授確認」的判斷仍然有效。
  - 實際動工後又踩到第二個問題：把 POI Verifier、Contingency Handler 各自包成 1 個 skill（等於重新包裝現有的 2 個 Agent），這正是本項第一行說的「現況」，不是「最小版本」——本項自己寫的最小版本明確是**拆到 `validators/`／`detectors/` 這一層**（約 15–18 個候選 skill），才接近教授說的「數十種」規模；包 2 個 Agent 等於什麼都沒變，只是多了文件。
  - 另外「Master Routing」本身也有兩種可能：（a）真的讓 LLM 依問題動態決定呼叫哪個 skill；（b）純粹是 README／架構圖上的靜態對照表（本項最小版本寫的「用現有模組化程度包裝敘事」比較像 (b)）。教授 07/22 舉的 Google Skill 框架範例聽起來像 (a)，但團隊目前的模組化程度撐不撐得起 (a) 沒有共識，這個落差沒問清楚就會重複做錯方向。
  - 結論：**這題的兩層歧義（要不要現在做／做到哪個顆粒度）都還沒解決，且都不是靠團隊內部猜測可以定案的，維持本項「先跟教授確認」的建議，且確認時要把上面三點一起問清楚，不要只問要不要做。**
- **2026-07-24 補充：「Skill 清單」與「拆 skill 的重構工程」要分開排序，前者不卡 #1**：複查後發現這兩件事常被誤認為同一件事、綁在一起等 #1 拍板，其實不是：
  - **Skill 清單本身不依賴 #1**。清單內容（`validators/`、`detectors/`、`evaluators/`…約 15–22 個候選項）是現有程式碼結構決定的，不是資料規模或地理範圍決定的——不管 #1 最後拍板「擴大」還是「集中中壢／央大」，程式碼裡有哪些模組都不會變。這部分（含 Master Routing 對照表，若走靜態表格版本）**現在就可以做，不需要等 #1**，甚至可以反過來當 #1／#20（解耦敘事）的佐證材料。
  - **真的卡 #1（更精確地說是卡工時排序，不是技術上做不到）的是「拆 skill 的重構工程」**——即把 `poi-verifier/src/enrichers/index.ts` 現有的單一 4-in-1 LLM 呼叫（facts + level + backup_logic + tourist_friendly_description 全部塞進一次 call）拆成獨立可鏈式呼叫的 skill。原因不是技術依賴，是兩個現實考量：① 工時稀缺，#1／#11 卡三次會議還沒解決，這個重構不是本週最高槓桿的事；② 在現有 45 筆測資上重構驗證意義有限，真正有代表性的驗證要等 #1 拍板後的實際匯入規模。
  - **附帶的 token 成本試算**（用 `results/poi_verified.json` 45 筆真實 `tokens_used` 均值 3,231 token／筆、Gemini 2.5 Flash 現行付費費率 input $0.30／output $2.50 每 1M token 推算，屬估算非實測）：現況單次融合呼叫 ≈ NT$0.10／筆；**天真拆分**（4 個獨立呼叫但重複帶入原始來源 JSON）反而更貴 ≈ NT$0.16／筆（+60%）；**串接式拆分**（只有 Skill 1 讀原始 JSON，Skill 2/4 吃 Skill 1 產出的精簡 facts，Skill 3 備案邏輯多數情況直接用既有規則函式 `generateBackupLogic()`、不呼叫 LLM）≈ NT$0.08／筆，略低於現況。**結論：成本本身在任何規模下都不是決定要不要做的關鍵因素**（45 筆時差距 < NT$10；就算是千筆 TDX 匯入規模差距也僅 NT$60–160，量級遠低於團隊會在意的門檻）——真正該用來決定「現在做或之後做」的是工時排序，不是省不省 token。
  - **建議排序**：① 現在／可並行：把 skill 清單（含既有 22 個候選模組命名 + 靜態路由對照表）寫成文件，不動程式碼；② #1／#11 拍板、TDX 匯入規模確定後：若仍認為值得投入，再做 `enrichers/index.ts` 的串接式拆分重構，並用真實匯入規模重新量測 token 成本，而不是套用 45 筆估算值。
- **2026-07-24 pipeline 實查：skill 化「值不值得」的答案，先被一個更根本的發現改寫**（直接讀 `agents/poi-verifier/src/` 與 `results/poi_verified.json` 得出，細節見 `KNOWN_ISSUES.md` 三筆 2026-07-24 條目）：
  - **最關鍵的發現與 skill 路由無關**：現行 `poi_verified.json` 45 筆中 **30 筆是靜默降級的 fallback**（`tokens_used` 前 15 筆有值、後 30 筆為 0，即批次跑到一半 Gemini Free Tier RPD-20 配額耗盡後全部走 `enrichers/index.ts:219` 的「LLM 不可用 → 預設 L2」分支）。這 30 筆的 `suggested_level=2` 是預設值不是判斷，`facts=null`、無 `tourist_friendly_description`——**目前教授看到的「45 筆驗證庫」實際上只有 15 筆真的被 LLM 驗過**。而且 `agent.ts` 沒把 `llm_source` 寫進輸出，下游完全分不出真 L2 與失敗 L2。這件事比「要不要拆 skill」急，也直接牽動 #1（範圍拍板時能不能信任既有 45 筆）與 #10（重新匯入前必須先重跑那 30 筆，否則把降級資料一起 embed）。
  - **哪些問題「真的」能靠 harness／skill 化修好**（值得投入的部分）：① 靜默降級——統一的 skill 回傳型別（`{value, source, confidence, degraded}`）會強制每個模組宣告出處，這是 30/45 問題的架構性根因，不是一次性 bug；② `enrich-llm` 4-in-1 拆分——一次 JSON parse 失敗現在會同時損失 facts+level+backup+description 四樣（30 次降級正是如此），拆開後只損失一樣；③ per-skill 快取讓「只補跑缺的 official_website／ptt／youtube」不必重燒 Google 配額（複查：這三個來源在現行 45 筆的 `sources` 出現次數為 0，P0/P1/P2 驗證器對現行資料集零貢獻）；④ **對畢業專題最有價值的**——統一介面才能做 ablation（抽掉 PTT → reliability_score 與真值的相關性變差多少），目前六來源權重（0.50/0.30/0.25/0.15/0.10/0.10）寫死在 `crossValidate` 內、從未對任何真值校準過。
  - **哪些問題「不能」靠 skill 化修好，不要用來當重構的理由**：#11 signals bug（純 5 行修正）；`batch-verify.ts` 每筆 12 秒（是 OSM 1 req/s 序列化阻塞整筆，需 per-source rate-limit queue，不是 skill 邊界）；**規則層覆蓋率**——§四／#20 敘事主打「多數決策在規則層、不綁 LLM」，但 `preClassifyLevel` 只在名稱／描述含 8 個預約關鍵字時觸發，45 筆只命中 2 筆，等於 level 分類 ~96% 仍靠 LLM，**這句敘事在教授複查程式碼時是最脆弱的一點，建議先補強規則或改口徑**；分數權重校準（無真值標籤，任何架構都救不了）。
  - **對 #14 兩層歧義的結論修正**：（a）「要不要現在做」——`enrichers/index.ts` 的兩個低風險前置改動（移除 prompt 中會被丟棄的 `backup_logic`、把 `llm_source` 傳進輸出並重跑 30 筆）**不是 skill 重構、應該先做**，做完再談拆 skill；⟶ **2026-07-26 更新：`llm_source` 持久化＋移除 prompt `backup_logic`＋修 L0 backup_logic 掛回三項程式改動已完成，僅剩重跑 30 筆待團隊拍板花費；並修正原文——只移 `backup_logic`，`suggested_level` 必須保留（`preClassifyLevel` 僅命中 2/45，其餘 ~96% 等級靠 LLM 的 `suggested_level`），理由見 `KNOWN_ISSUES.md` 2026-07-26 條目**；（b）「動態路由 vs 靜態對照表」——維持先前判斷：Stage 1–3 是真線性流程，demo 前不建議做動態路由（等於替只有一條路徑的管線蓋派工器）。skill 化的真正價值是「讓失敗可見 + 讓 ablation 可測」，不是路由本身。
  - **一句話結論（可對教授／團隊複誦）**：這條管線的問題不是「模組化不夠」，而是「失敗是隱形的」——45 筆有 30 筆悄悄降級卻當成資料入庫。harness engineering 值得做的那部分，正好就是把這種靜默降級變成顯性契約；動態 skill 路由則是 demo 後才需要評估的加分項。

### 15. Loop Engineering（自動爬文更新資訊）open question `後端` 新增（教授0715）

- **現況**：教授 07/15 提到的 Agentic Loop（系統自動定期去部落格／YouTube 爬文更新資訊）目前**沒有排程機制**——`agents/poi-verifier` 的驗證流程是 CLI 手動觸發（`CLAUDE.md` §9 進度矩陣：「只離線跑」），沒有 cron 或排程器定期重跑既有 POI 的驗證。
- **為什麼列為 open question 而非直接排工**：跟 #1（資料涵蓋範圍）系出同源——如果範圍決議是「集中在小範圍做深度驗證」，定期重爬的必要性和頻率會不一樣；如果是「擴大涵蓋」，反而應該先把首次匯入做完，重爬是次要。
- **建議討論**：這次會議不用拍板要不要做，但要決定「等 #1 拍板後，這個要不要排進 NIICC 計劃管理的階段 3-6」，避免忘記教授提過這件事。

### 16. 具體商業獲利模式與規模化故事（千萬產值敘事）`全隊` 新增（教授0715，2026-07-23 複查發現先前誤判為已解決）

- **現況**：先前版本的本文件把「商業模式」整項標記為 ✅ 已完成（見 #5），但複查 `系統架構圖_競賽版.md`「規模化與商業定位（Plug-in）」與 `NIICC_初審文件_企劃書.md` 全文後發現，兩份文件目前只論證**技術面**可規模化（pgvector 在數萬筆規模仍維持效能、單次檢索成本壓在 NT$5 以下），**完全沒有具體的獲利模式**——沒有寫「旅遊平台要付多少錢串接」「潛在市場規模多大」「訂閱制還是按量計費」。教授 07/15 明確要求的是「讓評審覺得這是有機會創造千萬產值的好生意」，這是商業論證，不是技術論證，兩者不能互相取代。
- **為什麼要修正優先度**：#5 當時標記已完成，容易讓人誤以為商業模式這塊已經沒有剩餘工作，但送件文件目前實際上只有 Plug-in 的「概念定位」，沒有「生意會不會賺錢」的具體數字或情境。既然 NIICC 送件文件已進入收尾階段（見 #5 收尾細節），這塊空白會在正式評審時被直接看穿。
- **建議討論**：由誰負責寫這段（可能需要對旅遊業或 OTA 平台的收費模式做基本市場調查）；至少要決定一個具體切入點，例如「每次 API 呼叫收費」「依串接平台的月活躍用戶數分級訂閱」等，並估一個粗略的潛在市場規模（例如台灣 OTA／旅行社數量 × 假設滲透率），不需要精確，但要有數字支撐「千萬產值」的說法，而不是純敘述。

### 17. RAG 資料前處理（Chunking）是否適用本專案 `後端` 新增（教授0722，2026-07-23 複查發現本文件從未收錄）

- **現況**：教授 07/22 提到建置 RAG 系統時必須把龐大文件（例如財報、旅遊指南）正確切割成小區塊（chunking）才能確保精準檢索。複查 repo 後發現這個詞只在 `agents/poi-verifier/docs/rag-architecture-analysis.md` 出現過一次，且只是描述 RAG 一般流程的其中一步（「文件 → 切塊（chunking）→ embedding → 向量資料庫」），**不是針對 Navigator 自己的資料型態做過的分析或決策**。
- **為什麼可能不是空白，但需要一句話能回答教授**：Navigator 目前 embedding 的對象是每筆 POI 的結構化描述與 enrichment 洞察（短文字，非長文件），`blog_snippets` 也只存萃取後的短洞察，不存部落格全文——如果資料本身就不是長文件，嚴格意義上的 chunking 可能真的不適用，但目前 repo 裡沒有任何文件明確講出「我們評估過 chunking，因為資料型態是短結構化記錄所以不需要」這句話，教授若當場問起，答不出來會顯得沒想過。
- **建議討論**：不需要真的去做 chunking，但建議在 `NIICC_初審文件_企劃書.md` 或 `系統架構圖_競賽版.md` 補一句話說明「為什麼我們的 RAG 不需要 chunking（資料型態是短結構化記錄，不是長文件）」，把這題防守起來；如果未來要處理部落格全文引用或更長的旅遊指南類資料，屆時再重新評估。

### 18. Context Window 管理／Hand-off 機制是否要做 `後端` 新增（教授0722，2026-07-23 複查發現本文件從未收錄）

- **現況**：教授 07/22 建議的 Hand-off 機制是「系統把學習到的經驗與重要狀態定期寫入獨立檔案，隨後重置對話狀態，下次直接從該檔案讀取核心知識」，用來避免長對話 Token 消耗過大或 AI 失憶。複查後發現這跟 `references/0513_教授回饋整理暨組員討論.md` 裡已標記「✅ 已解決」的 Context/Harness Engineering 是**不同層次的東西**——0513 那項解決的是「候選池太大塞爆單次 prompt」（`agents/contingency-handler` 把候選壓縮到 top-5 再送進 LLM），不是「多輪對話狀態的持久化與重置」。本文件先前也從未把這兩者的差異寫清楚。
- **為什麼列為 open question 而非直接排工**：Navigator 現有的兩個 Agent（POI Verifier、Contingency Handler）都是**單次事件觸發、無多輪對話記憶需求**的流程，不是那種會話越拉越長、需要定期交接狀態的長對話系統，目前看不出產品端有明確的使用場景需要 hand-off 機制。硬做一個目前用不到的機制，會撞上 07/08 教授自己說過的「不要在架構複雜度上鑽牛角尖」。
- **建議討論**：跟教授確認這個要求是不是特別針對「之後要做 Loop Engineering 自動爬文」（#15）之後才會出現的場景——背景爬蟲任務如果要長時間運行、分批處理大量景點，可能才會真的需要「進度寫檔、重啟後接續」的 hand-off 機制。如果是，可以先記錄「等 #15 排入時程時一併設計」，不需要現在生出一個沒有對應使用場景的機制。

### 19. 開發成本管控／經濟型訂閱方案 `全隊` 新增（教授0722，2026-07-23 複查發現本文件從未收錄）

- **現況**：教授 07/22 提醒團隊注意呼叫外部 LLM API 的成本控管，並舉例不同雲端／訂閱方案的經濟效益差異（例如每月約 20 美金的雲端方案，或六人分攤一年約 1000 元的團隊方案），目的是避免開發過程中出現非預期的雲端開銷。複查後發現 `API_KEY_PRICING.md` 只記錄**產品本身**呼叫外部 API（Gemini／Claude／Supabase／Mapbox 等）的費率，**沒有記錄團隊自己在開發過程中使用的 AI 輔助工具（例如 Claude Code 之類）訂閱方案是否已經是最經濟的選擇**，這是兩件不同的事，先前版本沒有區分清楚。
- **為什麼優先度低**：這比較像內部行政／財務事項，不影響任何開發方向或架構決策，跟本文件其他項目性質不同。
- **建議討論**：不需要展開討論，會議中花 2 分鐘確認目前團隊用的開發工具／訂閱方案是否合理即可；如果沒有踩雷（例如誤用高單價的按量方案而非團隊方案），這項可以直接結案不需要追蹤。

### 20. 解耦設計優勢（Decoupling）敘事補寫 `全隊` 新增（教授0722，2026-07-23 三次複查發現本文件從未收錄）

- **現況**：教授 07/22 提出「解耦設計的優勢」——系統明確拆分成資料層／推理層／應用程式介面，代表這是一個高彈性的「資料服務（Data Service）」，**不綁死於單一 LLM**，未來能輕鬆以 Plugin 形式掛到不同平台。這件事**技術上已經是事實**：`NIICC_初審文件_企劃書.md` 的技術清單已寫明「AI 主力為 Gemini 2.5／1.5 Flash，Claude Haiku 為結構化輸出備援」，本身就是「不綁死單一 LLM」的直接證據；架構也早就分層並以 `/api/poi/search`、`/api/contingency` 對外提供 Plug-in 端點。**但沒有任何文件把這兩個既有事實明確連成教授要的那句論述**——現有文件講 Plug-in 是從商業定位角度講（#5、#16），講多 LLM 備援是從技術穩定度角度講，沒有人從「解耦＝可規模化的學術／商業雙重論證」這個角度把兩者串起來寫成一段話。
- **為什麼歸類為敘事缺口而非技術缺口**：跟 #17（chunking）性質類似——不需要改架構、不需要寫新程式碼，純粹是「這句話目前沒人講出來」。跟 #13（Benchmark 學術定位）、#16（商業獲利敘事）放在一起看，教授 07/22 那段話其實是在給團隊三個現成但沒被講出來的加分論述，這是其中最後一個。
- **建議討論**：由誰負責在 `NIICC_初審文件_企劃書.md`「二、創意構想」或「三、系統架構」段落補一小段「解耦＝彈性資料服務」的論述，可以直接引用現有的 Gemini／Claude Haiku 雙模型備援與 `/api/poi/search`、`/api/contingency` 兩個獨立端點當證據，不需要新增功能，預估 10 分鐘內能寫完，可以跟 #16 排在同一次文件收尾工作裡一起做。

---

## 教授提醒（心態校準，非討論項）

- **07/08**：技術已經到堪用水準就該往下一階段推進，不要在資料衝突精準度或 RAG 架構複雜度上鑽牛角尖——外部資料本身就無法保證絕對正確。學校的評分機制未必能精準鑑別真實能力，AI 技術發展太快。多去外部比賽讓真實世界檢驗，企業對 AI 工具使用能力與產出效率的評鑑會直接且殘酷，及早培養「存活力」比在校內打磨完美更重要。
- **07/15**：不喜歡完全由 AI 生成、沒有團隊自己觀點的敷衍企劃書；企劃書內容要負責任，別被制式範本綁架，聚焦核心創新架構。系統提供的資訊必須非常精準，特別是突發狀況（颱風天等）系統絕對不能給出會害死人的錯誤資訊——資訊驗證與應變機制要做好。
- **07/22**：核心功能要聚焦，強烈支持砍除多人投票等發散性功能，把技術量能 100% 集中在資料的「收斂」與應變系統的「精準度」上（此點已於 0716 拍板落實）。呼叫外部 LLM API 要注意成本，測試階段選有經濟效益的方案，避免非預期雲端開銷。

---

## 建議會議流程（若時間有限，依此順序切）

1. `P0` #1 —— 15 分鐘內拍板資料涵蓋範圍（擴大 vs 集中），這是後面所有決議的前提。**這是連續第三次會議都要處理同一件事，建議這次會議前先各自帶著明確立場來，不要現場重新發散討論。**
2. `P0附屬` #11 —— 5 分鐘確認 signals bug 由誰修、什麼時候修完（必須早於任何一次重新匯入/回填）。
3. `P1` #2 / #10 —— 5 分鐘確認 TDX 執行時機與負責人（#1、#11 都解決後就是純執行）。
4. `P1` #4 剩餘部分 —— 10 分鐘拍板要不要做語意搜尋框 UI，不做也要記錄理由。
5. `P1` #4.5 —— 5 分鐘排入 sprint、指定負責人（後端管線和選點組行程都已是真資料，純接線工作，性價比最高）。
6. `新增` #16 —— 10 分鐘決定由誰補寫具體獲利模式與規模化數字，這是送件文件目前實際的空白，不是加分項，建議跟 #5 的收尾細節一起排時間。
7. `新增` #13、#14、#15 —— 15 分鐘討論這三項要不要排進 NIICC 計劃管理階段，特別是 #14 建議先跟教授確認期望的落地程度再動工。
8. `新增` #17、#18、#19、#20 —— 5 分鐘內快速過，多數只需要一句話防守、確認訂閱方案合理、或補一小段敘事即可結案，不用展開討論；#20 可以跟 #16 一起排給同一個人收尾。
9. `P3` —— 不占會議時間，會後各自認領即可（目前只剩 #10 卡在 #1/#11，其餘已完成或不排入）。
