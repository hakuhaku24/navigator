# 待討論事項與優先順序 — 2026-07-09

> 前身為 `待討論事項_0705.md`。本次更新反映兩件事：(1) Supabase 連線異常已排除，原 P0 阻塞項移除；(2) `系統架構圖_0709.md` 已產出並經教授核可，原本待畫的整合流程圖項目視為完成；(3) 7/8 教授講評帶來新的優先順序調整，詳見下方各項與文末「教授提醒」。
> 編號 = 優先順序，**數字越小越優先**、越可能是專案卡關的原因。同一個優先級內用小數區分（1.1、1.2…）。
> 來源對照：延續自 0705 文件編號會標註「原#x」；教授 7/8 講評新增的項目標註「新增（教授0708）」。

---

## 🔴 P0 — 阻塞全隊，今天/本次會議必須有結論

### 1. 資料涵蓋範圍拍板：擴大 vs 集中驗證 `全隊` 新增（教授0708）

- **現況**：目前 demo 資料僅 45 筆。教授 7/8 直接點出本專題的核心賣點是「用堪用的技術，涵蓋全台灣的景點」——廣度與資料量是差異化優勢，而非技術本身有多先進。教授要求針對資料量不足要有明確 Plan，並給了兩個方向：擴大景點涵蓋範圍，或縮小並集中（例如只鎖定中壢／央大附近）在小範圍內做到完整驗證。
- **為什麼最優先**：這個決議直接決定接下來 TDX 匯入、POI 驗證 pipeline 要往哪個地區／規模擴充，也牽動 Demo 主軸敘事（#3）與對外「施力點」說法（#6）。是目前唯一「沒拍板全隊會在錯的資料規模上做工」的項目。
- **今天要決議**：擴大還是集中？若集中，鎖定哪個地理範圍（中壢／央大周邊？）；若擴大，用什麼節奏與自動化程度去擴充。

---

## 🟠 P1 — 阻塞前端/後端下一步方向，不拍板會做出不相容或用不到的東西

### 2. TDX 正式匯入排程 `後端` （原#2，因 Supabase 已恢復、migration 008 已套用而改變性質）

- **現況**（2026-07-09 複查）：Supabase 連線異常已排除（原 0705 文件記錄的 DNS 解析失敗問題已解決）。migration 008 其實**已經套用完成**——`poi_catalog` 已有 9 個事實層欄位（`category`/`city`/`zip_code`/`curated_zone`/`hours`/`phone`/`images`/`website_url`/`source_update_time`），比 0705 文件記錄的狀態更進一步。目前資料表仍只有 45 筆（跟套用前一樣），新欄位全是 NULL，代表 schema 就緒但還沒真的匯入資料把欄位填起來。TDX OAuth 憑證已驗證可用，`ingest-from-tdx.ts` dry-run 正常，隨時可以真的執行匯入。
- **為什麼優先**：既然阻塞都排除了，剩下純粹是「匯入多大範圍」的問題，直接跟 #1 的資料範圍決議綁在一起——範圍沒拍板就先跑，很可能匯入到不需要的地區或筆數不夠，要重做。
- **今天要決議**：#1 拍板後，由誰執行 `npx ts-node ingest-from-tdx.ts`、跑多大規模（`--top`／`--city` 參數）、跑完後誰驗證資料正確。

### 3. Demo 主軸與敘事更新：可信資料庫廣度 vs 應變排程能力 `前端` `後端` （原#3，依教授意見調整框架）

- **現況**：教授 7/8 明確表態本專題最大價值在於「用堪用技術涵蓋全台灣景點」，廣度比技術深度更重要，不需要把 RAG 架構做到 Agentic RAG 等級的複雜度——「有招就好」，該往下一階段推進。同時 `系統架構圖_0709.md`（已核可）已完整規劃 Contingency Handler 應變流程，代表應變排程仍是明確的差異化亮點之一。
- **為什麼優先**：Demo 流程、UI 開發優先順序、簡報敘事都要跟著這個定位走，不拍板前端極可能做完要改。
- **今天要決議**：主敘事怎麼講（廣度覆蓋為主、應變排程為輔，或反之）；8 步 Demo 流程是否仍鎖定 Step 7（天氣應變 Swap）當展示重點。

### 4. LLM × RAG 前端串接與呈現方式 `前端` `後端` （原#4，架構已定案，落實待決）

- **現況**：架構層級已在 `系統架構圖_0709.md` 中定案並經教授核可（Hybrid Retrieval Engine 屬於 Architect Agent 的一環，SQL Search + pgvector Search → 行程建議）。後端 API 已完成：`src/app/api/poi/search/route.ts`（POST 完整參數／GET 快速查詢）與 `src/lib/poi-search.ts`。前端尚未串接此端點，LLM 回應的呈現方式（引用來源／信度標示）也還沒設計。
- **為什麼優先**：教授直接質疑「使用者端 LLM 到底有沒有讀到我們驗證的資料」，前後端接口不對齊會直接做出不相容的東西。
- **今天要決議**：前端怎麼呼叫這個 endpoint、怎麼呈現 LLM 回應（含引用來源／信度標示）。

### 5. 商業模式重新檢視 ＋ 尋找比賽簡章 `全隊` （原P2 #6 KKday 部分，因教授7/8意見與明確期限提升）

- **現況**：教授質疑原本設想的 KKday／Klook 抽佣模式在真實商業比賽中可能站不住腳，建議重新思考落地方式（例如：專屬諮詢服務、AI Skill、以 Plugin 形式融入現有平台），並要求下週開會前找到合適的比賽簡章，讓教授能針對該比賽屬性給具體方向。
- **為什麼優先**：有明確截止時間（下次開會），且商業邏輯的重新設計會回頭影響對外簡報與施力點說法（#6）。
- **今天要決議**：分工找簡章的人選與截止時間；初步討論除了抽佣之外還有哪些商業包裝方向可以先準備給教授看。

---

## 🟡 P2 — 需要方向共識，但不會立刻卡住寫 code

### 6. 跟教授同步「施力點」對外說法 `全隊` （原#5，教授已給方向，改為同步事項）

- **現況**：教授 7/8 已直接肯定「可信景點資料庫 ＋ 堪用技術涵蓋全台灣」是核心賣點，原本待拍板的「施力點是否分工太發散」疑慮已有明確答案。
- **剩餘工作**：把這個說法同步進簡報／README 等對外文件，跟 #1、#3 的決議結果對齊即可，不需要再開會辯論，可與 #3 同一次對話一起講掉。

### 7. LLaMA 地端部署 open question `後端` （原#6 剩餘部分）

- **現況**：LLaMA 地端部署是否排入時程，一直沒人給明確答案（排進時程 or 明確放棄並說明原因）。
- **為什麼中等優先**：懸而未決會持續耗費「要不要做」的心力，但目前沒有其他工作因此被卡住，可以這次會議花 5 分鐘直接拍板。

---

## 🟢 P3 — 已有共識或屬於執行細節，可並行處理、不需要專門開會

### 8. ~~多來源衝突的前端 UI 呈現~~ `前端` （原#7）✅ 已完成（2026-07-05）

- ~~後端衝突解析已完成（`conflict-resolver.ts`，45 筆全測過），只差前端怎麼標示「這個景點資料有分歧版本」。~~
- 已在 `src/app/(app)/explore/page.tsx` 實作：景點卡片顯示「資料分歧」徽章，詳情面板列出每個衝突欄位的採用值、判定方式與各來源原始值。資料由 `agents/poi-verifier/generate-frontend-conflicts.ts` 離線算出。教授 7/8 也認可這類「有判定規則即可」的做法，不需要再花時間追求絕對精準，此項維持已完成狀態即可。

### 9. ~~整合流程圖（景點驗證 → 資料庫 → 應變系統 → 使用者畫面）~~ `全隊` （原#9）✅ 已完成（2026-07-09）

- ~~不需要改 code，找個人畫一版初稿，其他人 review 即可。~~
- 已產出 `系統架構圖_0709.md`，涵蓋外部資料來源 → POI Verifier Agent → 知點資料庫（含 pgvector Hybrid Search）→ Architect / Contingency Handler Agent → 使用者介面的完整流程與回饋迴路，並經教授核可。此項移出討論清單。

### 10. 跑一次大量 TDX 正式匯入 `後端` （原#8）

- 純執行任務，不是決策。Supabase 已恢復，等 #1（範圍拍板）與 #2（套用時機）確定後直接跑，拿實際成功率數字回報教授即可。

### 11. 既有 45 筆回填 signals 缺口（Bug）`後端` 新增（2026-07-09 複查發現）

- **現況**：確認 migration 008 已套用後，原本想直接把既有 45 筆重新跑一次入庫來回填新欄位，但檢查 `ingestToDB()`（`src/ingestion.ts`）與三個既有入庫腳本後發現：**現在直接重跑既有 45 筆，`category` 與 `images` 這兩個新欄位還是會寫入空值**——不是資料不存在，是呼叫端沒把資料傳進去。
  - `batch-verify.ts:135`、`ingest-from-results.ts:58` 呼叫 `ingestToDB()` 時完全沒帶 `signals` 參數，即使原始資料 `references/測資Json.json` 每筆都有 `category` 與 `image_url`（例：`NCA-001` 有 `"category": "體驗活動"` 與真實圖片網址）也會被丟棄。
  - `ingest-with-p012.ts:142` 有組出 `IngestSignals`（PTT/YouTube/官網訊號），但同樣沒帶 `category`／`image_url`／`image_urls`；官網爬蟲明明已經找到 `official_website_url` 並存進 `metadata`，卻沒複製到新的頂層 `website_url` 欄位。
  - 不受影響：`city`／`curated_zone` 是 `ingestToDB()` 內部從 `region`／`address` 自動推導，不靠 signals，回填時會正確。`hours`／`phone`／`zip_code` 本來就不在 45 筆手寫資料裡，維持 null 合理，不是 bug。
- **為什麼要修**：如果沒發現就先跑回填，會誤以為「已經回填過」但 `category`/`images`/`website_url` 其實還是空的，之後要重新診斷一次、白費一輪 embedding API 成本。
- **今天不用決議**：純程式修正，補上 `signals` 裡 `category`/`image_url`/`website_url` 的傳遞即可，不需要開會；但要在真正執行既有 45 筆回填之前完成，否則回填等於白做。跟 #2（新增 TDX 匯入）是分開的兩件事——TDX 來源走的是 `tdx-mapper` 另一條路徑，不受此 bug 影響。

### 12. Booking enrichment 實作排程 `後端` （原#10）

- 規劃文件已完成，純粹是「排進哪個 sprint」的問題，優先度最低，可會議最後 1 分鐘定個時間點就好。

---

## 教授提醒（心態校準，非討論項）

- 技術已經到堪用水準就該往下一階段推進，不要在資料衝突精準度或 RAG 架構複雜度上鑽牛角尖——外部資料本身就無法保證絕對正確。
- 學校的評分機制未必能精準鑑別真實能力，AI 技術發展太快。多去外部比賽讓真實世界檢驗，企業對 AI 工具使用能力與產出效率的評鑑會直接且殘酷，及早培養「存活力」比在校內打磨完美更重要。

---

## 建議會議流程（若時間有限，依此順序切）

1. `P0` #1 —— 15 分鐘內拍板資料涵蓋範圍（擴大 vs 集中），這是後面所有決議的前提。
2. `P1` #2 —— 5 分鐘確認 TDX 套用時機與負責人。
3. `P1` #3 —— 20 分鐘主軸與敘事拍板（含教授「廣度優先」意見的落地方式）。
4. `P1` #4 —— 15 分鐘 API 串接方式對齊。
5. `P1` #5 —— 10 分鐘分工找比賽簡章、初步討論商業包裝方向。
6. `P2` #6、#7 —— 10 分鐘內拍板，不用展開討論。
7. `P3` —— 不占會議時間，會後各自認領即可。
