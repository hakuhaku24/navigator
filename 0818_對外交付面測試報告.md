# 對外交付面測試報告（2026-08-18）

透過 MCP server 的兩個 tool（`search_verified_pois`、`weather_contingency`）對線上系統做的黑箱測試，共 8 個 prompt、7 次工具呼叫。所有發現都附重現步驟。

- 測試路徑：MCP → REST `/api/plugin/*` → `poi_catalog`（即對外串接者實際會走的路徑）
- 線上資料狀態：100 筆，tier_2 64／tier_1 27／tier_0 9
- 程式碼版本：`1d21f8b`

> ⚠️ 本報告的 DB 數字為 2026-08-18 上午 service role 實查。若組員之間有人正在跑批次，請重查後再引用。

---

## 一、測試清單

| # | 測試 prompt | 呼叫 | 結果 | 觸發的發現 |
|---|---|---|---|---|
| 1 | 陽明山的室內景點 | `search_verified_pois`（list 模式） | 4 筆 | F1 |
| 2 | 北海岸適合拍照的景點 | `search_verified_pois`（hybrid 模式） | 8 筆 | F4, F6, F7 |
| 3 | 東北角信任分數最高的 3 個 | `search_verified_pois`（list 模式） | 3 筆 | F6 |
| 4 | 可信度怎麼判斷 | 無（讀原始碼） | — | — |
| 5 | 找衝突值得注意的景點 | 無（service role 查 DB） | 掃描 100 筆 | **F1** |
| 6 | NCA-004 下雨備案 | `weather_contingency` ×2 | 1 次參數錯誤 ＋ 1 次成功 | F2, F5, F8 |
| 7 | 降雨機率 90% | `weather_contingency` | 成功 | **F3** |
| 8 | NCA-999 下雨 | `weather_contingency` | 404（正確） | — |

**#4 和 #5 沒有經過 MCP／REST**，是直接讀 repo 與查 DB。若要當作 plug-in 的測試紀錄，實際覆蓋到的是 6 次成功呼叫。

**#8 的錯誤處理是對的**，值得保留：回 `404 找不到景點 NCA-999（已查詢 poi_catalog 與靜態資料）`，講明查過哪兩個地方，沒有回空清單、沒有退回預設景點、也沒有讓 LLM 就著不存在的 id 生成建議。與 `KNOWN_ISSUES.md` 記載的 `?? false` 那類「靜默降級」正好相反。

---

## 二、發現（依嚴重度）

### F1 [高] `is_open` 的衝突裁決 5 筆全錯，且錯法對稱

全庫 100 筆的 `conflict_analysis` 中，只有 **5 筆**是系統真的做了裁決（其餘為 `coexist` 並列或無衝突）。這 5 筆全部在 `is_open` 欄位，**全部裁反**。

| 景點 | 官網抓取 | Google Places | 系統裁決 | 實際 |
|---|---|---|---|---|
| 大屯自然公園 YMS-009 | 失敗 | OPERATIONAL | ❌ 已歇業 | 開放中 |
| 冷水坑溫泉浴室 YMS-010 | 失敗 | OPERATIONAL | ❌ 已歇業 | 開放中 |
| 淺水灣海濱公園 NCA-008 | 失敗 | OPERATIONAL | ❌ 已歇業 | 開放中 |
| 舊草嶺隧道 NEI-005 | 失敗 | OPERATIONAL | ❌ 已歇業 | 開放中 |
| 草山行館 YMS-001 | 成功 | **CLOSED_TEMPORARILY** | ❌ 營業中 | **休館中** |

前四筆是偽陰性，第五筆是偽陽性。**唯一一筆真的歇業的景點，是唯一一筆被判成營業中的。**

**根因**：`agents/poi-verifier/src/conflict-resolver.ts:301`

```ts
if (v.official_website) {
  out.push({
    value: v.official_website.is_reachable,
    source_name: '官方網站（可連線 = 仍在營運）',
    source_tier: 'official',
    confidence: 0.90,
```

守衛是 `if (v.official_website)`，但 `agents/poi-verifier/src/validators/official-website.ts:128` 在**抓取失敗時仍回傳物件**，只是 `is_reachable: false`。於是「HTTP 請求掛掉」被包裝成一個 `official` 層、信度 0.90 的權威主張。再加上 `conflict-resolver.ts:112` 的 `TIER_GAP_FOR_CLARIFICATION = 1`，official(4) 比 semi_official(3) 高 1 層即可 `clarified_by_tier`，直接輾過 Google。

那個來源名稱本身就寫出了缺陷：「可連線 = 仍在營運」，這個等號雙向都不成立。連不上不代表歇業（逾時、擋爬蟲、網址過期），連得上也不代表還開著（草山行館）。

**目前影響範圍為 0**：`grep is_open src/` 顯示前端只在 `explore/page.tsx:83` 的欄位名稱對照表用到它，沒有任何篩選或警示邏輯讀取。所以這 4 筆錯誤現在不會害到使用者，但草山行館那筆也同樣得不到保護——這與 `search_verified_pois` 仍會回傳 `is_open: false` 的石門洞（NCA-010）是同一個狀況：**算了，但沒有人用**。

好消息是修的時候沒有既有行為要顧；壞消息是一旦有人接上去用（例如加「可能已歇業」警示），現在就會有 4 個開放景點被標成歇業。

**建議修法**：守衛改成 `if (v.official_website?.is_reachable)`——只在抓得到時才發言，抓不到就不投票，讓 Google 的 `single_source` 勝出。這比調 tier 權重乾淨：問題不在官網該不該優先，而在「抓取失敗」根本不該被當成一個關於營業狀態的判斷。建議同時補一個測試蓋住「官網抓取失敗時不應影響 is_open」。

草山行館那筆要另外處理：真正的訊號在 TDX 描述裡（「自 114 年 11 月 11 日起暫停開放」），而 `isOpenVariants()` 的註解明講 TDX 被排除在 `is_open` 之外，理由是「歇業登記要幾個月才更新」。該理由對「已登記歇業」成立，對「官方描述裡寫著暫停開放」不成立。

**重現**：見附錄 A。

---

### F2 [高] L0／需預約景點會被推薦為臨時雨天備案

`weather_contingency(NCA-004, 0.8)` 的第 5 名候選是 **陽明書屋（YMS-003）**，L0 絕對錨點、定時導覽制。臨時下雨走過去進不去。

系統自己的 `level_reasoning` 就寫著「旅客無法自由入場，必須配合固定時段的導覽」，但候選篩選沒有把「需預約」排除在臨時備案之外。

L0 的既有規則是「不能被自動換掉」，這裡是反方向——**換過去**同樣有問題，而這個方向沒有被任何規則涵蓋。它距離 14.6 km、`distance_score` 已經是 0，仍靠室內（`weather_fit: 100`）與評分排進前五。

**這是本次發現中唯一會讓使用者真的撲空的一項。**

**建議**：候選檢索階段排除 `level === 0` 或 `business_logic.requires_reservation === true` 的景點。

**重現**：`weather_contingency(poi_id: "NCA-004", rainfall_probability: 0.8)`，看 `recommended_contingencies[4]`。

---

### F3 [中] 事件嚴重程度不影響任何下游決策

降雨 0.8 與 0.9 的輸出**完全相同**：

| | 0.8 | 0.9 |
|---|---|---|
| 期望值 | 14 | 9.5 |
| 分數落差 | 36 | 40.5 |
| 通過候選 | 11 | 10 |
| 首選／前五名 | 劉家肉粽富基店 … 陽明書屋 | **完全相同** |
| 策略 | `swap_poi` | `swap_poi` |
| 影響評估 | +15 分／中性 | **+15 分／中性** |

**根因**：`agents/contingency-handler/src/generators/contingency-plan-generator.ts:140`

```ts
const hasGood = !!top && top.multi_criteria_score >= 60
if (event.kind === 'weather') {
  return hasGood ? { type: 'swap_poi', ... } : { type: 'delay_timeslot', ... }
}
```

策略只看首選候選分數有沒有到 60，與 `score_drop`、`rainfall_probability`、`event_severity` 都無關。首選是 87.4，離門檻很遠，所以從觸發門檻（落差 20）一路到 100% 降雨，輸出都是同一份。

`expected_value_analysis` 唯一的作用是決定「要不要觸發」這個布林值；觸發之後嚴重程度就被丟掉了——不影響候選排序（`weather_fit` 室內即 100，與雨量無關）、不影響策略選擇、不影響影響評估（+15 分鐘是寫死的常數）。

**對 demo 的風險**：若教授連問「那 60% 呢」「那 95% 呢」，畫面會給出一模一樣的答案，而我們的敘事是「期望值推理驅動決策」。

**建議（最小可行）**：把 `score_drop` 接進策略選擇——落差極大時除了 `swap_poi` 一併建議 `delay_timeslot`，或讓 `impact_assessment.group_satisfaction_impact` 隨落差變化。改的是同一個函式，不動候選檢索。

**附帶**：`multi_criteria_score` 的六項準則中，`availability_score`(100)、`crowd_capacity_score`(80)、`group_preference_match`(60) 對所有候選皆為常數。實際參與排序的只有 `weather_fit`、`distance_score`、`rating_score` 三項。所謂「多準則」目前有一半不影響結果。

---

### F4 [中] hybrid 搜尋模式不回傳 `curated_zone`

同一筆 POI（NEI-008 潮境公園），兩種模式回傳不同：

| 模式 | 呼叫 | `curated_zone` |
|---|---|---|
| list（空 query） | `search_verified_pois(region: "東北角")` | `"東北角"` ✅ |
| hybrid（有 query） | `search_verified_pois(query: "海邊步道", region: "東北角")` | `null` ❌ |

DB 實際值為「東北角」。**不是資料缺失，是 `hybrid_search` RPC 的回傳欄位沒有帶上 `curated_zone`。**

DB 現況（100 筆）：陽明山 35／東北角 34／北海岸 27／NULL 4。NULL 那 4 筆是 CLAUDE.md 已記載、刻意保留的（滿月圓、內洞、烏來台車、永和樂華夜市），與本問題無關。

**影響**：任何依 `curated_zone` 做判斷的串接方，只要走語意搜尋就會拿到 null。CLAUDE.md 記載 explore 的區域篩選已於 2026-08-04 改讀 `curated_zone` 並移除 `?? "北海岸"` 靜默預設——若 explore 的搜尋路徑也吃這個 RPC，需確認有沒有回到「篩不到」的狀態。

**重現**：

```
search_verified_pois(region: "東北角", top: 2)                    → curated_zone 有值
search_verified_pois(query: "海邊步道", region: "東北角", top: 2) → curated_zone 為 null
```

---

### F5 [中] 應變回應的 `region` 取自 `city` 而非 `curated_zone`

`weather_contingency` 回傳的候選中，乾華十八王公廟顯示 `"region": "新北市"`，而同一批其他候選顯示「北海岸」。

DB 實查：該筆 `curated_zone = "北海岸"`、`city = "新北市"`。**資料是對的，是回應組裝時取錯欄位**，且只對部分候選發生（推測是 `curated_zone` 為空時 fallback 到 `city`，但此筆並非空值——實際邏輯請組員確認）。

**影響**：串接方看到的區域標籤不一致，同一份候選清單裡混著「北海岸」與「新北市」兩種粒度。

**重現**：`weather_contingency(poi_id: "NCA-004", rainfall_probability: 0.8)`，看 `recommended_contingencies[2].region`。

---

### F6 [中] `category` 欄位未經衝突解析，直接取 OSM 值

| 景點 | `category` | 問題 |
|---|---|---|
| 老梅綠石槽 NCA-004 | `restaurant` | OSM 配到隔壁「綠石槽平價海鮮餐廳」 |
| 八斗子 NEI-012 | `bus_stop` | 實為台鐵深澳線車站 |
| 金山老街 NCA-005 | `viewpoint` | 老街不是觀景點 |
| 神秘海岸 NCA-007 | `viewpoint` | 同上 |
| 陽明書屋 YMS-003 | `yes` | OSM 標籤值沒清乾淨漏出來 |
| 乾華十八王公廟 | `自然景觀` | 廟宇；且與 `space_type: indoor` 矛盾 |

值得注意的是**衝突解析機制本身是對的**：老梅綠石槽的 `official_name` 誠實記錄了 `coexist`（Google 說「老梅綠石槽」、OSM 說「綠石槽平價海鮮餐廳」），麟山鼻木棧道的 OSM 地址配到士林區碧山路也照實留著，沒有靜默覆蓋。問題是 **`category` 根本沒進這條流程**。

**建議**：把 `category` 納入 `conflict-resolver`，或至少在 OSM 值與 Google `types` 衝突時標記。

---

### F7 [低] `blog_posts` 相關性汙染，且不限於 YouTube

實際出現在佐證清單裡的內容：

| 景點 | 汙染內容 | 來源標記 |
|---|---|---|
| 陽明書屋 | 「2025 in anime - Wikipedia」、勞斯萊斯開箱 | `duckduckgo` |
| 中山樓 | 勞斯萊斯開箱 | `duckduckgo` |
| 石門婚紗廣場 | 「#2025 YouTube」、「#2025 TikTok」 | `duckduckgo` |
| 神秘海岸 | 澳洲旅行社評測 | `duckduckgo` |
| 潮境公園 | 南投埔里景點懶人包 | `duckduckgo` |
| 八斗子 | 國家鐵道博物館（台北市） | `duckduckgo` |

**與既有記載的差異**：CLAUDE.md 既有問題 4 描述為「YouTube 佐證混入不相關影片」，並註明 `youtube_video_count` 線上為 0 所以「這個問題現在不會出現在畫面上」。**實測顯示該結論不成立**——汙染來源標記全是 `duckduckgo`，走的是部落格搜尋路徑，現在就出現在畫面上。

**建議**：相關性過濾要加在 `blog-search` 而不只是 `youtube-search`。最低門檻：標題或摘要須含景點名稱或別名。

---

### F8 [低] MCP tool schema 缺 `rainfall_probability` 的單位說明

首次呼叫時照字面傳 `rainfall_probability: 80`（80%），被擋下：

```
400 參數錯誤：rainfall_probability Too big: expected number to be <=1
```

驗證有做、訊息也清楚，但 MCP 的 tool schema 裡這個參數只有 `{"type": "number"}`，**沒有 description、沒有標單位、沒有 min/max**。呼叫端的 AI 只能猜是 0–1 還是 0–100。錯誤訊息友善是因為底層 zod 擋住了，schema 層面其實沒說。

**建議**：`mcp-server/server.js` 的 tool schema 補 `description: "0–1 之間的機率值，例如 0.8 代表 80%"`，並加 `minimum: 0, maximum: 1`。同步更新 `PLUGIN_API.md`。

---

## 三、觀察（不算缺陷，但建議討論）

1. **八斗子 NEI-012 的縣市歸屬有爭議**：`city` 為「新北市」（採 Google「新北市瑞芳區」），OSM 則為「基隆市中正區北寧路」。座標 (25.135, 121.802) 距同批潮境公園（基隆市）約 900 公尺。八斗子車站實際位在基隆市中正區。**這是資料決策，建議由人決定權威來源後再改**，不建議自動修。

2. **應變管線沒有季節性概念**：老梅綠石槽的觀賞期是 3–5 月，八月推薦它本身就不合適。季節資訊目前只寫在 `description` 與 `level_reasoning` 的自由文字裡，沒有結構化欄位承載，所以判不出來。

3. **UV 指數與降雨在同一個 event 物件裡自相矛盾**：`uv_index: 11`（極危險）與大雨特報並存。UV 是 CWA 當日最大值預報，不隨當下雲量調整。真實情境兩邊都取真值時也會有這個問題。

4. **候選淘汰數會隨時間變動**：相隔 76 秒的兩次呼叫，通過候選從 11 變 10（多一筆「即將打烊」）。時間相關判定本來就會如此，但意味著**同一 demo 情境在不同時間跑會給出不同候選數**，上台前要知道。

5. **`fine_probability` 有浮點誤差**：0.8 時為 `0.19999999999999996`。目前只影響顯示，若日後拿去與門檻做等號比較會踩雷。

6. **推薦的體驗類型不對等**：老梅綠石槽（季節限定海蝕地景、拍照類）的首選備案是劉家肉粽富基店（小吃店）。層級與區域都符合 Swap 定義，但沒有任何 `decision_tags.vibe` 的比對——而 POI schema 裡是有這個欄位的。與 F3 的「三項準則為常數」是同一件事的兩面。

---

## 四、建議優先序

| 順序 | 項目 | 理由 | 規模 |
|---|---|---|---|
| 1 | **F2** L0／需預約排除出備案池 | 唯一會讓使用者真的撲空的 | 小 |
| 2 | **F1** `official_website` 守衛 | 資料正確性根因，且現在改沒有既有行為包袱 | 一行 ＋ 測試 |
| 3 | **F4** RPC 補 `curated_zone` | 影響串接方與 explore 搜尋路徑 | 小 |
| 4 | **F8** schema 補單位 | 對外文件，成本最低 | 極小 |
| 5 | **F3** 嚴重程度接進決策 | demo 敘事一致性 | 中 |
| 6 | F5／F6／F7 | 顯示品質 | 中 |

---

## 附錄 A：衝突掃描腳本

在 repo 根目錄放到暫存資料夾執行即可，不需寫進 repo。需要 `.env` 的 `SUPABASE_URL` 與 `SUPABASE_SERVICE_ROLE_KEY`。

```js
import fs from 'fs'
const env = Object.fromEntries(fs.readFileSync('.env','utf8').split('\n')
  .filter(l => l.includes('=') && !l.trim().startsWith('#'))
  .map(l => [l.slice(0, l.indexOf('=')).trim(),
             l.slice(l.indexOf('=') + 1).trim().replace(/^["']|["']$/g, '')]))

const URL = env.SUPABASE_URL, KEY = env.SUPABASE_SERVICE_ROLE_KEY
const rows = await (await fetch(
  `${URL}/rest/v1/poi_catalog?select=source_id,name,verification_tier,conflict_analysis&limit=200`,
  { headers: { apikey: KEY, Authorization: `Bearer ${KEY}` } })).json()

for (const p of rows) {
  const ca = p.conflict_analysis
  if (!ca) continue
  for (const [field, rec] of Object.entries(ca)) {
    if (rec?.is_conflicted && rec.resolution_method !== 'coexist') {
      console.log(p.source_id, p.name, field, rec.resolution_method, '→', rec.resolved)
      for (const v of rec.variants ?? []) {
        console.log('   ', `[${v.source_tier}]`, v.source_name, '=', v.value)
      }
    }
  }
}
```

⚠️ 注意：`reliability_score` **不是** `poi_catalog` 的頂層欄位（直接查會回 `42703 column does not exist`），它存在 `metadata` 裡，由 API 層取出後才出現在回應中。
