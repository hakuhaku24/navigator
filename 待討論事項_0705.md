# 待討論事項與優先順序 — 2026-07-05

> 根據 `待討論並完成部分_0702.md` 整理。目的：把「單純還沒做」跟「不討論/不拍板就會卡住其他人」分開，後者才是這次要優先排時間討論的。
> 編號 = 優先順序，**數字越小越優先**、越可能是專案卡關的原因。同一個優先級內用小數區分（1.1、1.2…）。
> 來源對照：`(一-x)` `(二-x)` `(建議x)` 對應 0702 文件裡的原始編號，方便回頭查細節。

---

## 🔴 P0 — 阻塞全隊，今天/本次會議必須有結論

### 1. Supabase 連線異常確認 `後端` （一-1／二-1）

- **現況**：資料庫網址連 DNS 都解析不到，不是單純休眠，可能專案被刪或網址換了。
- **為什麼最優先**：只要沒解決，PTT／官網／YouTube 查證資料寫入、TDX 正式匯入、新欄位套用全部卡住 —— 是目前唯一「一人沒空確認、全隊停工」的項目。
- **需要誰**：有 Supabase 後台管理權限的組員。
- **今天要決議**：確認專案是否還在、要不要重建、重建的話資料要不要重新匯入。

### 2. TDX 新欄位套用時機協調 `後端` （一-2／二-2）

- **現況**：資料庫設定檔已寫好，但還沒套用。
- **為什麼優先**：若跟舊版匯入程式同時跑會互相打架、弄亂資料，是「不講清楚就會出事」的協調問題，而非單純待辦。
- **依賴**：需等 #1 Supabase 恢復後才能真的套用，但「誰、什麼時候套」這個協調現在就該講好，避免恢復後手忙腳亂。
- **今天要決議**：套用的人、套用前要跟誰喊一聲、套用後誰負責驗證資料正確。

---

## 🟠 P1 — 阻塞前端/後端下一步方向，不拍板會做出不相容或用不到的東西

### 3. Demo 主軸拍板：POI 驗證可靠性 vs 應變排程能力 `前端` `後端` （建議三＋建議四合併討論）

- **現況**：教授肯定 Alternative Route（應變行程）方向，但主打哪個功能沒定案；跟既有系統（TripAdvisor / Google Places / MindTrip / WanderNest）的差異化調查也還沒做。
- **為什麼優先**：0702 文件自己就標了「⚠️ 主軸未定前前後端都不要動」—— Strategy Agent Swap UI mock、天氣觸發場景設計全部依賴這個結論，先做前端極可能做完要改。這是目前唯一明確寫著「會員擋在原地等決策」的項目。
- **建議做法**：全組 30 分鐘短會，2-3 人分頭查競品，本次會議直接拍板「我們的獨特性說法」+ 主打功能。
- **今天要決議**：主打 POI 可靠性還是應變排程；8 步 Demo 流程是否鎖定 Step 7（天氣應變 Swap）當展示重點。

### 4. LLM × RAG 整合方式（Route Handler）`前端` `後端` （建議二）

- **現況**：RAG Reranker + Hybrid Search 已在 agent 內部完成，但尚未接進前端 `/api/` Route Handler。
- **為什麼優先**：這是教授直接質疑的點（「使用者端 LLM 到底有沒有讀到我們驗證的資料」），且前後端接口不對齊會直接做出不相容的東西 —— 跟 #3 一樣是「不談就會做錯方向」而不是「還沒空做」。
- **今天要決議**：串接哪個 agent endpoint、前端怎麼呈現 LLM 回應（含引用來源／信度標示）。

---

## 🟡 P2 — 需要方向共識，但不會立刻卡住寫 code

### 5. 跟教授拍板「施力點」說法 `全隊` （二-3）

- **現況**：內部已認知施力點是「可信景點資料庫 ＋ 應變系統」，但還沒正式跟教授講清楚、消除「分工太發散」的印象。
- **為什麼中等優先**：影響教授觀感與報告措辭，但不影響組員今天手上的 code 進度。跟 #3 的結論高度相關，建議在同一次對話一起講掉。

### 6. LLaMA 地端部署 / KKday／Klook 抽成 open question `後端` （二-6）

- **現況**：兩個問題一直沒人給明確答案（排進時程 or 明確放棄並說明原因）。
- **為什麼中等優先**：懸而未決會持續耗費「要不要做」的心力，但目前沒有其他工作因此被卡住，可以這次會議花 5 分鐘直接拍板。

---

## 🟢 P3 — 已有共識或屬於執行細節，可並行處理、不需要專門開會

### 7. ~~多來源衝突的前端 UI 呈現~~ `前端` （建議一剩餘工作）✅ 已完成（2026-07-05）

- ~~後端衝突解析已完成（`conflict-resolver.ts`，45 筆全測過），只差前端怎麼標示「這個景點資料有分歧版本」。可由前端自行設計後跟後端對一次資料格式（`variants` / `is_conflicted`）即可，不必等大會。~~
- 已在 `src/app/(app)/explore/page.tsx` 實作：景點卡片顯示「資料分歧」徽章，詳情面板列出每個衝突欄位的採用值、判定方式與各來源原始值。資料由新增的 `agents/poi-verifier/generate-frontend-conflicts.ts` 離線算出（不呼叫外部 API），詳見 `待討論並完成部分_0702.md` 建議一。此項移出討論清單，不需要占用會議時間。

### 8. 跑一次大量 TDX 正式匯入 `後端` （二-4）

- 純執行任務，不是決策，等 #1 #2 解決後直接跑，拿實際成功率數字回報教授即可。

### 9. 整合流程圖（景點驗證 → 資料庫 → 應變系統 → 使用者畫面）`全隊` （二-5）

- 不需要改 code，找個人畫一版初稿，其他人 review 即可，不必占用會議時間逐字討論。

### 10. Booking enrichment 實作排程 `後端` （一-3）

- 規劃文件已完成，純粹是「排進哪個 sprint」的問題，優先度最低，可會議最後 1 分鐘定個時間點就好。

---

## 建議會議流程（若時間有限，依此順序切）

1. `P0` #1、#2 —— 5 分鐘內確認狀態與負責人，不深入討論細節，卡住就先散會去修。
2. `P1` #3 —— 30 分鐘主軸拍板（含建議三的競品調查分工）。
3. `P1` #4 —— 15 分鐘 API 串接方式對齊。
4. `P2` #5、#6 —— 10 分鐘內拍板，不用展開討論。
5. `P3` —— 不占會議時間，會後各自認領即可。
