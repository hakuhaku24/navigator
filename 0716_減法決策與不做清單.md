# 0716 減法決策 — 不做清單（給全組）

> **一句話**：做東西太多做不完，組內已拍板做減法。系統定位從「多人旅遊的智能共識＋即時韌性」收斂為**「可信景點資料庫＋即時韌性應變」**——這也正是教授 7/8 說的施力點。
> **原則**：只縮範圍、不刪程式碼。已寫好的頁面全部保留在 repo，日後要復活隨時可以；但**不再開發、不進競賽敘事、不排工時**。

---

## ❌ 不做了（多人規劃全鏈，已拍板）

| 不做的東西 | 對應檔案 | 說明 |
|---|---|---|
| 建立行程房間 | `src/app/group/new/` | 程式碼保留，停止開發 |
| 加入行程房間 | `src/app/group/[id]/join/`、`JoinModal.tsx` | 同上 |
| 代幣投票（VETO / MUST-GO / Like） | `src/app/(app)/trip/[id]/vote/` | 同上 |
| 投票收斂結果頁 | `src/app/(app)/trip/[id]/results/` | 同上 |
| 多人即時同步（Realtime presence）、`trip_votes` 資料表 | 尚未動工 | **直接從待辦刪除，不要開工** |
| draft-itinerary 吃投票結果 | `src/lib/draft-itinerary.ts` | 行程草案本身保留，但輸入改為單人選點（見下方拍板 2） |

## 🧊 凍結／延後（不主動做、不入架構圖）

| 項目 | 處理 |
|---|---|
| Tinder swipe 滑卡頁（`trip/[id]/explore`） | **留程式碼、不入架構圖與簡報**（下游投票沒了） |
| `ai-plan` 通用 AI 行程生成頁 | 凍結——最容易被評審看成「ChatGPT 包皮」，別展示 |
| `collection`、`settings`、`dashboard` | 凍結／降為簡單入口，不投工時 |
| **Supabase Auth 登入整合** | **延後賽後**——demo 不需要登入 |
| traffic / venue / group 三個應變偵測器 | 維持 stub 永久凍結（demo 只做天氣） |
| Booking enrichment | 從排程刪除 |
| 回饋迴路／賦能（偏好學習） | 延後賽後 |
| 多準則排序 11 權重 | 待瘦身為 5–6 個有真實資料的準則 |
| PWA | 維持停用（既有決策） |

## ✅ 只做這條主線（其他都不要碰）

```
① 外部來源(8 API) → ② POI 驗證(交叉驗證/衝突解析/信任評分/L0-L3)
→ ③ poi_catalog 知識庫(混合檢索) 
→ ④ 應變系統(CWA 偵測 → 期望值 → RAG 備案 → 反思審查 → LLM 生成)
→ ⑤ 三個視圖：驗證檢視 / 應變建議 / 風險地圖
```

主線上還要做的具體工作（優先序）：
1. 前端接上 `/api/poi/search`（explore 頁，含信度/來源呈現）——教授質疑的核心
2. 應變 agent 接成 API route + weather 頁去 mock 化（主打 demo）
3. explore 驗證庫加「加入行程」選點（單人行程來源，見拍板 2）
4. ingestion signals bug 修復（`category`/`images`/`website_url` 漏傳）——**45 筆回填前必修，否則白跑**
5. TDX 匯入（等範圍拍板）

## 📌 三個連帶拍板

1. **swipe 頁**：留 code、不入架構圖。
2. **應變 demo 的行程來源**：使用者從驗證庫選點成行程（explore 加「加入行程」→ 簡單排序 → 行程），取代投票收斂。
3. **Auth**：延後賽後。

## ❓ 常見問題

- **我寫的 vote/group 頁會被刪嗎？** 不會。程式碼全部保留，只是停止開發、不進敘事。
- **企劃書/簡報要改嗎？** 要。凡是把「多人共識/投票」寫成現行功能的段落都要改掉或移到「未來展望」。需求書已同步為 v0.3（FFR1/2/6/7 標移除）、CLAUDE.md 已更新。
- **為什麼砍多人？** 工時不足＋教授施力點明確（可信資料庫＋應變）＋競賽敘事收斂（Gemini 建議亦同向）。差異化不靠多人，靠「驗證過的字典＋反思審查過的應變」。

---

*連動文件：`CLAUDE.md`（§1/§2/§7/§9 已同步）、`2026…系統需求書.md`（v0.3）、`系統架構圖_競賽版.md`（無投票版，製作中）。0707 拍板見 `docs/decisions-0707.md`。*
